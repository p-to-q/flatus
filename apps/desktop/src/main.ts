// Source of truth: `main.js`.
//
// The desktop shell loads the plain JS module directly so the Tauri webview
// stays build-step free. Keep this file around only as a signpost for editors
// and future refactors.
export {};
